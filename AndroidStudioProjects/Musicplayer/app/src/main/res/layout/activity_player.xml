<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.mainpc.musicplayer.Player"
    android:background="#298d7b">

    <SeekBar
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/seekBar"
        android:background="#dedbd6e5"
        android:clickable="false"
        android:visibility="visible"
        android:layout_below="@+id/prev_button"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:text="&lt;&lt;"
        android:id="@+id/rev_button"
        android:layout_marginBottom="60dp"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/play"
        android:layout_alignEnd="@+id/play"
        android:layout_marginRight="42dp"
        android:layout_marginEnd="42dp" />

    <Button
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:text=">>"
        android:id="@+id/fwd_button"
        android:layout_below="@+id/play"
        android:layout_toLeftOf="@+id/next_button"
        android:layout_toStartOf="@+id/next_button" />

    <Button
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:text="||"
        android:id="@+id/play"
        android:layout_above="@+id/rev_button"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:text="//>>/"
        android:id="@+id/next_button"
        android:layout_above="@+id/seekBar"
        android:layout_alignRight="@+id/seekBar"
        android:layout_alignEnd="@+id/seekBar" />

    <Button
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:text="//&lt;&lt;/"
        android:id="@+id/prev_button"
        android:layout_alignBaseline="@+id/rev_button"
        android:layout_alignBottom="@+id/rev_button"
        android:layout_toLeftOf="@+id/rev_button"
        android:layout_toStartOf="@+id/rev_button" />

</RelativeLayout>
